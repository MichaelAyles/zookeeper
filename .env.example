# OpenRouter API Configuration (for AI features)
VITE_OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxx
VITE_OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
VITE_TEXT_MODEL=google/gemini-3-flash-preview
VITE_IMAGE_MODEL=google/gemini-2.5-flash-image

# Backend Configuration
# -----------------------
# Backend uses Cloudflare D1/R2/Pages Functions
# Configure via wrangler.toml and Cloudflare dashboard secrets:
#
# 1. Set GOOGLE_CLIENT_ID in wrangler.toml [vars] section
# 2. Set secrets via CLI:
#    wrangler secret put GOOGLE_CLIENT_SECRET
#    wrangler secret put JWT_SECRET
#
# For local development:
#    npm run dev:api
